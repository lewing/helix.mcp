# 2026-03-01 — Release version checklist

**Requested by:** Larry Ewing

v0.2.0 release required a fix because `server.json` wasn't updated alongside the csproj version bump. The publish workflow caught the mismatch. A release checklist decision was captured in `decisions.md` to prevent recurrence.
